package com.hananel.workschedule.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Brand Colors ────────────────────────────────────────────────────────────
// Change these to instantly update all teal elements app-wide
val PrimaryTeal = Color(0xFF00897B)      // Main brand teal (Material Teal 600, more vibrant)
val PrimaryTealDark = Color(0xFF00695C)  // Gradient end — used in all teal buttons

// ─── Semantic Colors ──────────────────────────────────────────────────────────
val BlockedRed  = Color(0xFFEF5350)      // Blocking / Shabbat observer / delete
val CanOnlyBlue = Color(0xFF1976D2)      // "Can only work" restrictions
val Orange      = Color(0xFFFF9800)      // Edit mode / Mitgaber / draft warnings

// ─── Home Screen Card Accent Colors ─────────────────────────────────────────
val CardBlue        = Color(0xFF1565C0)  // History card
val CardBlueDark    = Color(0xFF0D47A1)  // History card gradient end
val CardEmerald     = Color(0xFF00796B)  // Employees card (deep teal-green)
val CardEmeraldDark = Color(0xFF00574B)  // Employees card gradient end
val CardViolet      = Color(0xFF5E35B1)  // Template settings card
val CardVioletDark  = Color(0xFF4527A0)  // Template settings gradient end

// ─── Supporting / Legacy Colors ──────────────────────────────────────────────
val PrimaryGreen = Color(0xFF4CAF50)     // Theme tertiary
val PrimaryBlue  = Color(0xFF2196F3)     // Theme secondary
val Purple       = Color(0xFF9C27B0)     // Saving mode buttons
val Yellow       = Color(0xFFFFC107)     // Selection borders
val GrayLight    = Color(0xFFE8F5E9)
val GrayMedium   = Color(0xFF757575)

// ─── Adaptive Text Colors ────────────────────────────────────────────────────
val TextSecondary     = Color(0xFF666666)
val TextSecondaryDark = Color(0xFFB3B3B3)
val TextTertiary      = Color(0xFF999999)
val TextTertiaryDark  = Color(0xFF8A8A8A)
